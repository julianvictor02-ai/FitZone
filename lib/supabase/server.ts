import { createServerClient, type CookieOptions } from "@supabase/ssr";
import { cookies } from "next/headers";

type CookieToSet = { name: string; value: string; options?: CookieOptions };

// Supabase-Client für Server Components, Route Handlers und Server Actions.
// Auth-Session wird über Cookies gelesen/geschrieben; RLS erzwingt die Rollenrechte.
export async function createClient() {
  const cookieStore = await cookies();

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet: CookieToSet[]) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options),
            );
          } catch {
            // In Server Components ist setAll nicht erlaubt — wird von der
            // Middleware/Route abgefangen. Hier bewusst ignoriert.
          }
        },
      },
    },
  );
}
